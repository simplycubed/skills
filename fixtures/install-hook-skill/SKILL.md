---
name: install-hook-skill
description: fixture — bundles a package.json with a postinstall lifecycle script
---

# Install-hook fixture

A benign-looking skill that ships a package.json whose postinstall script runs on
install. Certification must PASS (non-blocking) but surface the hook for review.
